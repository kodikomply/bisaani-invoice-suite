import { hash } from "bcryptjs";
import { db } from "../../../lib/db";
import { requireUser } from "../../../lib/auth";

export async function GET() { const auth = await requireUser(["admin"]); if (auth.response) return auth.response; const rows = await db()`SELECT id, full_name, email, role, is_active, last_login_at, created_at FROM users ORDER BY created_at DESC`; return Response.json(rows); }
export async function POST(request: Request) { const auth = await requireUser(["admin"]); if (auth.response) return auth.response; const body = await request.json(); if (!body.fullName || !body.email || !body.password || body.password.length < 10) return Response.json({ error: "Name, email and a password of at least 10 characters are required" }, { status: 400 }); try { const rows = await db()`INSERT INTO users(full_name,email,password_hash,role) VALUES(${body.fullName},${body.email.toLowerCase()},${await hash(body.password,12)},${body.role || "staff"}) RETURNING id,full_name,email,role,is_active`; await db()`INSERT INTO audit_logs(user_id,action,entity_type,entity_id,details) VALUES(${auth.user!.id},'user.created','user',${String(rows[0].id)},${JSON.stringify({ email: rows[0].email, role: rows[0].role })})`; return Response.json(rows[0], { status: 201 }); } catch (e) { return Response.json({ error: e instanceof Error && e.message.includes("unique") ? "A user with that email already exists" : "Could not create user" }, { status: 409 }); } }
